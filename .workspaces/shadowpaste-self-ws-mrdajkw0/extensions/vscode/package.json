{
  "name": "shadowpaste-vscode",
  "displayName": "ShadowPaste for VS Code",
  "description": "Scan your workspace for leaked secrets, virtualize them into the ShadowPaste vault, and connect Cursor/Claude via MCP — same detector regex as the ShadowPaste backend.",
  "version": "0.1.0",
  "publisher": "shadowpaste",
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": ["Security", "Other"],
  "main": "./out/extension.js",
  "activationEvents": [
    "onCommand:shadowpaste.scanWorkspace",
    "onCommand:shadowpaste.protectSecrets",
    "onCommand:shadowpaste.connectMcp"
  ],
  "contributes": {
    "commands": [
      {
        "command": "shadowpaste.scanWorkspace",
        "title": "ShadowPaste: Scan Workspace for Secrets",
        "category": "ShadowPaste"
      },
      {
        "command": "shadowpaste.protectSecrets",
        "title": "ShadowPaste: Protect Secrets in Active Document",
        "category": "ShadowPaste"
      },
      {
        "command": "shadowpaste.connectMcp",
        "title": "ShadowPaste: Show MCP Config (Claude Desktop / Cursor)",
        "category": "ShadowPaste"
      }
    ],
    "configuration": {
      "title": "ShadowPaste",
      "properties": {
        "shadowpaste.serverUrl": {
          "type": "string",
          "default": "http://localhost:3000",
          "description": "Base URL of the ShadowPaste backend (the Next.js app). All API calls are made to relative paths under this URL — never hardcoded absolute URLs."
        },
        "shadowpaste.apiKey": {
          "type": "string",
          "default": "",
          "description": "Optional eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzaGFkb3ciOiJzYWZlIiwidGVzdCI6dHJ1ZX0.shadow9LoaQfCEhtZgNKrlEaXF sent as Authorization: Bearer <apiKey>. Used for authenticated scans and private-repo access."
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "@types/vscode": "^1.80.0",
    "typescript": "^5.3.0"
  },
  "license": "MIT"
}
